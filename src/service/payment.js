const paymentService = {
    // 发红包
    sendRedPackets(text) {
        // 调用支付接口
        alert(text)
    },
    // 转账
    transfer(text) {
        // 调用支付接口
        alert(text)
    },
};

export default paymentService;